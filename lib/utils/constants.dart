import 'package:flutter/material.dart';

class AppConstants {
  // Đường dẫn assets
  static const String jsonPath = 'assests/data/output.json';
  static const String logoPath = 'assests/image/logo.png';

  // Palette màu sắc cho Dark Mode mặc định
  static const Color backgroundDark = Color(0xFF121212);
  static const Color cardDark = Color(0xFF1E1E1E);
  static const Color primaryColor = Color(0xFFFF9F43); // Neon Orange sang trọng
  static const Color accentColor = Color(0xFF00D2D3);  // Neon Cyan
  static const Color textPrimaryDark = Color(0xFFF5F5F5);
  static const Color textSecondaryDark = Color(0xFFB0B0B0);
}
